import { css, theme } from '@graphiql-prototype/ui-library';

export const StyledInputObject = css({
  marginTop: theme.space[1],
  marginBottom: theme.space[1],
  display: `flex`,
  alignItems: `center`,
  width: `100%`,
});
