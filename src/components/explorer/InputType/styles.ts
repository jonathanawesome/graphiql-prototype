import { styled } from '@stitches/react';

export const InputTypeStyled = styled('div', {
  // border: '1px solid pink',
});

export const InputTypeChildArguments = styled('div', {
  borderLeft: `1px solid $scale100`,
  paddingLeft: 16,
  marginLeft: 7,
});
