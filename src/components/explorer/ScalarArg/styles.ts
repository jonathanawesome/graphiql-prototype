import { styled } from '@stitches/react';

export const ScalarArgStyled = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'flex-start',
  gap: 8,
  marginLeft: 1,
  marginTop: 20,
});
