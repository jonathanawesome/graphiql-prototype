import { Gear } from '@graphiql-prototype/graphiql-ui-library';
import { Settings } from './src';

export const DialogPluginSettings = {
  dialogPluginContent: Settings,
  dialogPluginIcon: Gear,
  dialogPluginName: 'Settings',
};
