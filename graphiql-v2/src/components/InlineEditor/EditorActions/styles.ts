import { styled } from '@graphiql-v2-prototype/graphiql-ui-library';

export const EditorActionsStyled = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  gap: 16,
  width: 40,
  minWidth: 40,
});
