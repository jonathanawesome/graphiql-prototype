import { styled } from '../../../theme';

export const EditorStyled = styled('div', {
  height: '100%',
  width: '100%',
  position: 'relative',
});

export const MonacoWrap = styled('div', {
  height: '100%',
  width: '100%',
});
